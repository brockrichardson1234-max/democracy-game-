# POP0-I5 — Implementation Authority

Status: **POP0-I5 IMPLEMENTATION AUTHORIZED. POP0-I6+, PLAYABLE UI, PRODUCTION MIGRATION, AND MAIN-BRANCH CHANGES UNAUTHORIZED. NOT POP0-I5 IMPLEMENTATION ACCEPTANCE.**

Authority chain:

- accepted POP0-I4 authority: `9def41f987a81de2cdc25b610eb169df9a666b5d`;
- controlling final repaired I5 design: `docs/product-evidence/35_POP0_I5_EXECUTABLE_CONTRACT.md` at `20e584b57bd0ec559c7c27babb79874e1c1825a4`;
- final detached I5 design re-audit: `fcebe27b416f97fa4d7e502fad7f11e5423ba4ec` — **PASS**;
- accepted production merge base and frozen `main`: `44c1724962830225e6fc34f41d0df0cfdb7dfec0`.

---

# 1. Authorized proof question

POP0-I5 may implement only the bounded concurrent-world proof defined by the controlling contract: can Employment, Congress, governors, organizations, media, a quiet maternity-service condition, and the inherited Housing workstream progress concurrently without becoming one scripted sequence, one shared mind, or one presidential command surface?

---

# 2. Authorized implementation

The implementation may add only the contract's exact I5 owners, configuration, identities, authorities, transitions, evidence routes, autonomous decisions, four workstreams/four escalation rules, three new rule-local presidential instrument families, HHS-only proactive inquiry, OMB-only review-period collision, deterministic same-instant advancement, reference-only history, and atomic `0.5.0-pop0-i5` / schema 5 / save-format 5 persistence.

The direct Employment, legislative, maternity-service, implementation, and Housing states remain their own canonical owners. Office, presidential, adapter, media, workstream, Attention, and historical records may observe or route only through the exact authority and receipt boundaries in the contract; they may not duplicate or create underlying domain truth.

The congressional adapter is gate-only. Its exact lower sequence is:

```text
beginSponsorSearch()
→ seekMemberSponsorship()
→ introduceSponsoredProposal()
→ advanceIntroducedProposalToGate()
```

One end-exclusive outer window may authorize at most one attempt for each transition kind. Only direct `LegislativeRuntimeState` owns sponsorship, introduction, proposal stage, and consideration-gate truth. No synthetic lower occurrence may be created.

---

# 3. Hard prohibitions

This authority does not permit:

- POP0-I6 or later work;
- playable UI or player-facing full canonical state;
- a Population or aggregate public-belief owner;
- generalized staff, politician, governor, organization, or media AI;
- a global player-action catalogue or generic proactive presidential command surface;
- presidential ownership of Employment, Congress, media, service access, implementation, or Housing outcomes;
- an I10 whole-session import, wrapper, synchronized save, or duplicate canonical state;
- Stage 1 or opening-usability lineage;
- production migration, succession, rollback, generated prehistory, or `main` modification.

---

# 4. Required verification and stop boundary

During implementation, targeted I5 suites and `verify:pop0-bounded` are development feedback only. Before review, the final candidate must run the complete canonical verification gate once at its exact SHA and provide the contract-required ownership, autonomy, counterfactual, hostile, concurrency, persistence, lower-owner, artifact, I10, boundary, merge-base, and unchanged-`main` evidence.

The implementation must stop for detached POP0-I5 implementation review. It may not author its own implementation acceptance or begin POP0-I6.

---

# 5. Authority disposition

## **POP0-I5 IMPLEMENTATION IS AUTHORIZED WITHIN THIS EXACT BOUNDARY.**

This document records the project authority explicitly supplied after the final detached design PASS. It authorizes coding only; it does not pre-accept the resulting implementation.
